<% if (locals.isAuthenticated) { %>
  <h1>New Trip Report</h1>
  <main class="container">
    <div class="row">

      <section class="col-md-7">
        <div class="map">
          <div class="mapEdit" id="map" data-lat="51.515209" data-lng="-0.072132"></div>
        </div>
      </section>

    <aside class="col-md-5">

      <form method="POST" action="/trips">
        <div class="form-group">
          <p> Come on tell us what happened! Maybe we will kiss it better. Click the marker on the map where you want to record your fall and then tell us what type of trip occurred. Then give it a name, and also a description about what went down</p>

        </div>
        <div class="form-group">
          <label for="latitude">Latitude</label>
          <input type="text" class="form-control" name="latitude" id="latitude" value="" readonly >
        </div>

        <div class="form-group">
          <label for="longitude">Longitude</label>
          <input type="text" class="form-control" name="longitude" id="longitude" value="" readonly >
        </div>

        <div class="form-group">
          <label for="title">Title</label>
          <input type="text" class="form-control" name="title" id="title" value="" required >
        </div>

        <div class="form-group">
          <label for="fallType">Fall type</label>
        </div>

        <div class="form-group">
          Trip <input type="radio" name="fallType" value="trip">
          Slip <input type="radio" name="fallType" value="slip">
          Stumble <input type="radio" name="fallType" value="stumble">
          Tumble <input type="radio" name="fallType" value="tumble">
          Fall <input type="radio" name="fallType" value="fall">
        </div>

        <div class="form-group">
          <label class="label"for="description">Tell us about your trip</label>
          <textarea class="input" type="text" name="description" id="description"></textarea>
        </div>
        <button type="submit" class="btn btn-primary btn-block">Submit</button>
      </form>
    </aside>
</div>
<% } else { %>
<div class="home">
  <h1><a href="/login">Log in</a> to have access to this content </h1>
</div>
</main>
<% } %>
